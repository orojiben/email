#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mailgun-email-validation/bin/mailgun-validate" "$@"
  ret=$?
else 
  node  "$basedir/../mailgun-email-validation/bin/mailgun-validate" "$@"
  ret=$?
fi
exit $ret
